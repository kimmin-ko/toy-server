pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version '2.3.0'
    }
}
rootProject.name = 'toy-server'

// gRPC API modules
include 'product-grpc'

// MSA services
include 'product'
include 'order'

// API Gateway
include 'gateway'

// Load test
include 'load-test'
