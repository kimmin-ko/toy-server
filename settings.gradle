pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version '2.3.0'
    }
}
rootProject.name = 'toy-server'

// gRPC API modules
include 'product-api'

// MSA services
include 'product'
include 'order'

// Load test
include 'load-test'
